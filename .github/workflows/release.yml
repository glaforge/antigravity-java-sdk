name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  JAVA_VERSION: '21'
  JAVA_DISTRO: 'temurin'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRO }}
          cache: maven

      - name: Sync Harness Binaries
        run: ./sync-harness.sh

      - name: Release
        env:
          JRELEASER_MAVENCENTRAL_USERNAME: ${{ secrets.JRELEASER_MAVENCENTRAL_USERNAME }}
          JRELEASER_MAVENCENTRAL_TOKEN: ${{ secrets.JRELEASER_MAVENCENTRAL_TOKEN }}
          JRELEASER_GPG_PASSPHRASE: ${{ secrets.JRELEASER_GPG_PASSPHRASE }}
          JRELEASER_GPG_PUBLIC_KEY: ${{ secrets.JRELEASER_GPG_PUBLIC_KEY }}
          JRELEASER_GPG_SECRET_KEY: ${{ secrets.JRELEASER_GPG_SECRET_KEY }}
          JRELEASER_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "GitHub Action"
          git config --global url."https://x-access-token:${GITHUB_TOKEN}@github.com/".insteadOf "git@github.com:"
          ./mvnw -ntp -B -Prelease release:prepare release:perform

      - name: Verify Bundle Integrity
        run: |
          JAR_FILE=$(find target/checkout/target/staging-deploy -name "antigravity-sdk-wrapper-*.jar" ! -name "*-sources.jar" ! -name "*-javadoc.jar" | head -n 1)
          echo "Checking wrapper JAR: $JAR_FILE"
          if [ -z "$JAR_FILE" ] || [ ! -f "$JAR_FILE" ]; then
            echo "ERROR: Wrapper JAR not found in target/checkout/target/staging-deploy!"
            exit 1
          fi
          JAR_SIZE=$(wc -c < "$JAR_FILE" | tr -d ' ')
          echo "JAR size: $JAR_SIZE bytes"
          if [ "$JAR_SIZE" -lt 50000000 ]; then
            echo "ERROR: antigravity-sdk-wrapper JAR is smaller than 50MB ($JAR_SIZE bytes). Embedded Go harness binaries appear to be missing!"
            exit 1
          fi
          jar tf "$JAR_FILE" | grep "google/antigravity/bin/" || {
            echo "ERROR: google/antigravity/bin/ missing from wrapper JAR!"
            exit 1
          }
          echo "Verified: Go harness binaries are present in wrapper JAR."

      - name: Publish with JReleaser
        env:
          JRELEASER_MAVENCENTRAL_USERNAME: ${{ secrets.JRELEASER_MAVENCENTRAL_USERNAME }}
          JRELEASER_MAVENCENTRAL_TOKEN: ${{ secrets.JRELEASER_MAVENCENTRAL_TOKEN }}
          JRELEASER_GPG_PASSPHRASE: ${{ secrets.JRELEASER_GPG_PASSPHRASE }}
          JRELEASER_GPG_PUBLIC_KEY: ${{ secrets.JRELEASER_GPG_PUBLIC_KEY }}
          JRELEASER_GPG_SECRET_KEY: ${{ secrets.JRELEASER_GPG_SECRET_KEY }}
          JRELEASER_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd target/checkout
          ./mvnw -ntp -B -Ppublication -N jreleaser:full-release

      - name: JReleaser output
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: jreleaser-release
          path: |
            target/checkout/target/jreleaser/trace.log
            target/checkout/target/jreleaser/output.properties
